name: Sync Repo Labels
on:
  push:
    branches:
      - master
    paths:
      - .github/labels.yml
  # Run manually.
  workflow_dispatch:

jobs:
  repo_setup:
    runs-on: ubuntu-latest
    steps:
      - uses: HeaSoLab/repo/.github/template/repo_sync_labels.yml@main
        with:
          repo-name: 'HeaSoLab/template'